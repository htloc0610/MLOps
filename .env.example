# ============================================================================
# LOCAL ML PIPELINE CONFIGURATION - EXAMPLE
# ============================================================================
# This is an example configuration file for the local ML pipeline.
# Copy this file to .env and modify the values as needed.

# ============================================================================
# MODEL HYPERPARAMETERS
# ============================================================================

# Random Forest hyperparameters
N_ESTIMATORS=100        # Number of trees in the forest (default: 100)
MAX_DEPTH=10            # Maximum depth of trees (default: 10)
RANDOM_STATE=42         # Random seed for reproducibility (default: 42)

# ============================================================================
# DATA SPLIT CONFIGURATION
# ============================================================================

# Train/test split ratio
TEST_SIZE=0.2           # Proportion of dataset for testing (default: 0.2 = 20%)

# ============================================================================
# PATHS (Optional - uses defaults if not set)
# ============================================================================

# Dataset paths (relative to project root)
# DATA_DIR=data
# DATA_FILE=Housing.csv

# Output paths (relative to project root)
# OUTPUT_DIR=output
# MODELS_DIR=output/models
# METRICS_DIR=output/metrics
# ARTIFACTS_DIR=output/artifacts
# PLOTS_DIR=output/plots

# ============================================================================
# ADVANCED MODEL PARAMETERS (Optional)
# ============================================================================

# Uncomment to customize further:
# MIN_SAMPLES_SPLIT=2     # Minimum samples required to split a node
# MIN_SAMPLES_LEAF=1      # Minimum samples required at each leaf node
# MAX_FEATURES=auto       # Number of features to consider for best split

# ============================================================================
# USAGE EXAMPLES
# ============================================================================

# Example 1: Train with more trees and deeper trees
# N_ESTIMATORS=200
# MAX_DEPTH=15

# Example 2: Use different random seed
# RANDOM_STATE=123

# Example 3: Use larger test set
# TEST_SIZE=0.3

# ============================================================================
# DOCKER USAGE
# ============================================================================
# When running with Docker, you can override these values:
#   docker run -e N_ESTIMATORS=200 -e MAX_DEPTH=15 mlops-pipeline
# Or in docker-compose.yml under the 'environment' section
